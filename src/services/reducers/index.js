import { combineReducers } from "redux";

const initialState = {
  data: [],
  constructorIngredients: [],
  currentIngredient: {},
  createdOrder: {},
};

export const rootReducer = combineReducers({});
